&НаКлиенте
Процедура ОтключитьМикрофон(Команда)
	ФормаНабора = ПолучитьФорму("Обработка.бит_Битфон.Форма.ОсновнаяФорма");
	ФормаНабора.КнОМик(Неопределено);
	Если ФормаНабора.МикрофонВключен = Ложь Тогда
		ПоказатьОповещениеПользователя("Микрофон",,"Успешно отключен",БиблиотекаКартинок.Информация32);
	Иначе
		ПоказатьОповещениеПользователя("Микрофон",,"Успешно включен",БиблиотекаКартинок.Информация32);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура УдержаниеВызова(Команда)
	ФормаНабора = ПолучитьФорму("Обработка.бит_Битфон.Форма.ОсновнаяФорма");
	ФормаНабора.КнУдержатьВызов(Неопределено);
	Если Если ФормаНабора.ИдетРазговор = Ложь Тогда
		ПоказатьОповещениеПользователя("Удержание",,"Вызов удержан",БиблиотекаКартинок.Информация32);
	Иначе
		ПоказатьОповещениеПользователя("Удержание",,"Вызов возвращен",БиблиотекаКартинок.Информация32);
	КонецЕсли;
КонецПроцедуры
